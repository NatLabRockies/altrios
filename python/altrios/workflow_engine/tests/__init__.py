# Per-primitive unit tests for the workflow engine. These tests are
# domain-neutral and do NOT exercise the freight catalog in
# ``altrios.lifts`` — they verify the engine in isolation so a
# domain-specific change can never silently break the engine's
# invariants.
